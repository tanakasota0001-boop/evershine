---
import { Image } from 'astro:assets';
import { siteSettings } from '../config/siteSettings';

const { about } = siteSettings;
const { content, style } = about;
---

<section id="about" class={`relative ${style.paddingY} overflow-hidden bg-white`}>
  <!-- Background Ornamental Elements -->
  <div class="absolute top-0 left-0 w-full h-px bg-gradient-to-r from-transparent via-gold/25 to-transparent"></div>
  <div class="absolute bottom-0 left-0 w-full h-px bg-gradient-to-r from-transparent via-gold/25 to-transparent"></div>

  <div class="px-6 md:px-12 lg:px-20">
    <div class="max-w-7xl mx-auto">
      <!-- Left-aligned Section Header -->
      <div class="about-reveal flex items-center gap-5 mb-14 md:mb-18">
        <span class="w-12 md:w-16 h-px bg-gold/60 flex-shrink-0"></span>
        <div class="flex flex-col gap-1.5">
          <span class="font-sans text-[clamp(1.8rem,4vw,3rem)] font-light tracking-[0.05em] text-slate-900 leading-tight">
            {content.sectionLabelJa}
          </span>
          <span class="font-display text-sm sm:text-base md:text-[17px] tracking-[0.3em] text-gold uppercase leading-none font-bold">
            {content.sectionLabel}
          </span>
        </div>
      </div>

      <!-- Main Editorial Layout -->
      <div class={`grid ${style.gridCols} gap-12 lg:gap-20 xl:gap-24 items-center`}>
        <!-- Left Column: Premium Detailing Studio Image (フレーム削除・クリーンな写真表示) -->
        <div class={`${style.leftColSpan} about-reveal relative group overflow-hidden rounded-sm shadow-2xl shadow-slate-900/10`}>
          <div class="relative overflow-hidden aspect-[4/5] w-full">
            <Image
              src={content.image}
              alt={content.imageAlt}
              class="w-full h-full object-cover transition-transform duration-[2s] ease-out group-hover:scale-105"
              widths={[500, 800, 1200]}
              sizes="(max-width: 768px) 100vw, 45vw"
              quality={90}
            />
            <!-- Subtle overlay -->
            <div class="absolute inset-0 bg-gradient-to-t from-slate-900/15 to-transparent pointer-events-none"></div>
          </div>
        </div>

        <!-- Right Column: Editorial Text & Headings -->
        <div class={`${style.rightColSpan} flex flex-col justify-center lg:pt-0`}>

          <!-- Main Heading -->
          <h2 class={`about-reveal font-display ${style.headingSize} font-light leading-[1.3] tracking-[0.02em] text-gold-dark mb-8 whitespace-pre-line lining-nums`}>
            {content.heading}
          </h2>

          <!-- Paragraphs -->
          <div class="space-y-7">
            {content.paragraphs.map((p, index) => {
              const isLast = index === content.paragraphs.length - 1;
              const pClass = isLast ? style.highlightTextColor : style.textColor;
              return (
                <div class="about-reveal">
                  <p class={`font-sans ${style.paragraphSize || "text-base md:text-lg lg:text-[1.15rem] leading-[2.3] tracking-[0.04em]"} ${pClass} whitespace-pre-line`}>
                    {p}
                  </p>
                </div>
              );
            })}
          </div>

          <!-- Signature-style ending -->
          <div class="about-reveal mt-12 flex items-center gap-4">
            <span class="w-12 h-px bg-gold/30"></span>
            <span class="font-display text-base md:text-lg tracking-[0.22em] text-[#0b1a30] italic font-semibold">{content.signature}</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  gsap.utils.toArray<HTMLElement>('.about-reveal').forEach((el, i) => {
    gsap.fromTo(
      el,
      { opacity: 0, y: 30 },
      {
        opacity: 1,
        y: 0,
        duration: 1,
        ease: 'power3.out',
        delay: i * 0.1,
        scrollTrigger: {
          trigger: el,
          start: 'top 85%',
          toggleActions: 'play none none none',
        },
      }
    );
  });
</script>
